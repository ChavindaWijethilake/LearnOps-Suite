{
  "name": "learnops-suite",
  "version": "1.0.0",
  "private": true,
  "description": "LearnOps Suite - Unified learning and service management platform",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18"
  }
}
