{
  "name": "learnops-suite",
  "version": "1.0.0",
  "private": true,
  "description": "LearnOps Suite - Unified learning and service management platform",
  "scripts": {
    "dev": "pnpm --filter @learnops/web dev",
    "dev:all": "pnpm -r --parallel dev",
    "build": "pnpm --filter @learnops/web build",
    "start": "pnpm --filter @learnops/web start",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18"
  }
}